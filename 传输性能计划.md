# 大文件传输性能

目标：带宽和磁盘允许时，真实两机 Wi-Fi 文件夹持续吞吐达到至少 20 MB/s。现场输入为 4 个 Word 文件，总计约 130 MB，当前约 6 MB/s。

- [x] P01 四文件约 130 MiB 和 1 GiB 本机样本完成基线与 SHA-256 校验。
- [x] P02 接收文件缓冲从默认 4 KiB 改为 1 MiB，合并网络短读产生的小写入；保留逐文件 Flush(true) 和批次提交。发送双缓冲未显示稳定收益，已撤回。增加发送阶段耗时、完成后的平均速度和提示。
- [x] P03 v5.1.1 传输实现与 v5.1.0 双向 HTTP、文件夹/中文/空文件、同时连接、P2P 16 MiB 和取消清理通过；相同大文件样本通过哈希校验。
- [ ] P04 真实两机 Wi-Fi 达到 20 MB/s 尚未验证，需要双方升级后复测同一文件夹。

不以本机回环或 Wi-Fi 协商速率冒充真实无线吞吐，不关闭磁盘提交或安全防护来提高数字。

## 证据与复测

2026-09-12，本机 Wi-Fi 协商发送 351 Mbps、接收 390 Mbps，TCP 自动调优 normal。无法读取另一台电脑的协商速率或磁盘性能。

两个隔离进程、本机回环、随机二进制样本（并非原 Word 文件），包含对方落盘确认：原版本四文件两轮 245–312 MB/s、1 GiB 两轮 210–603 MB/s；接收缓冲方案首轮分别约 276、309 MB/s。磁盘缓存、系统负载造成显著波动，不能据此声称提升百分比或 Wi-Fi 达标。这也不能排除远端设备、无线竞争、VPN 路由或文件安全扫描造成瓶颈。

双方运行 output/FindYou.Performance.exe（设置显示 5.1.1），复测同一个约 130 MB 文件夹。完成后记录“平均速度”，悬停完成状态查看总耗时、读取等待、网络写入等待、保存确认。平均速度包含扫描与对方提交时间；UI 沿用二进制 MB 单位，基准脚本 MB/s 为十进制。网络写入等待包含 TCP 背压，可能来自无线链路或接收侧磁盘，不能单独认定是哪一端。

最终构建复测四文件约 240 MB/s、1 GiB 约 321 MB/s，全部 SHA-256 一致；实际 HTTP 传输进行中取消与接收临时文件清理通过，独立程序启动退出通过。发送预读未保留，因此结果仅代表接收缓冲优化与诊断版。

传输统计日志仅添加协议、字节数和耗时，不包含文件名、路径或设备地址。P2P 的读写分项未记录，不应以零值解释为无耗时；界面对此仅显示总时间。

若仍约 6 MB/s，使用相同两台电脑和同一文件夹，将一台改为网线连接进行对照，同时记录另一台 Wi-Fi 协商速率。当前不能承诺原无线拓扑必达 20 MB/s，也不会将未确认的吞吐作为正式发布承诺。
