# 大文件传输性能

目标：带宽和磁盘允许时，真实两机 Wi-Fi 文件夹持续吞吐达到至少 20 MB/s。现场输入为 4 个 Word 文件，总计约 130 MB，最初约 6 MB/s；用户最新反馈约 11 MB/s，已达到其可用标准，希望缩短起步阶段。

- [x] P01 四文件约 130 MiB 和 1 GiB 本机样本完成基线与 SHA-256 校验。
- [x] P02 接收文件缓冲从默认 4 KiB 改为 1 MiB，合并网络短读产生的小写入；保留逐文件 Flush(true) 和批次提交。发送双缓冲未显示稳定收益，已撤回。增加发送阶段耗时、完成后的平均速度和提示。
- [x] P03 v5.1.1 传输实现与 v5.1.0 双向 HTTP、文件夹/中文/空文件、同时连接、P2P 16 MiB 和取消清理通过；相同大文件样本通过哈希校验。
- [ ] P04 真实两机 Wi-Fi 达到 20 MB/s 尚未验证，需要双方升级后复测同一文件夹。

不以本机回环或 Wi-Fi 协商速率冒充真实无线吞吐，不关闭磁盘提交或安全防护来提高数字。

## 起步延迟与速度显示（5.1.2）

输入：用户反馈速度随传输时间增加。依赖：现有 HTTP 长连接、5.1.1 接收缓冲和提交协议。输出：减少应用层等待、显示近期吞吐，维持取消和完成状态准确。

- [x] P05 查阅 TCP RFC、微软 HTTP 文档和 LocalSend 发送源码；区分 TCP 起步、信令等待与累计平均值。
- [x] P06 在首次设备探测前配置 Expect100Continue=false、NoDelay 与 KeepAlive；本机探针确认 GET、信令 POST、文件 POST 共用一条连接，信令不再等待 100 Continue。
- [x] P07 HTTP 流式发送与接收采用最近约 1 秒速度；准备阶段不纳入近期样本，停滞 1 秒归零，完成后仍显示含对方提交的平均速度。接收速率变化也触发刷新。P2P 按整窗口确认，保留累计平均值，提示说明口径。
- [x] P08 速度的准备/恒速/停滞/恢复/并发/样本数量检查通过；首包探针、130 MiB 文件夹和 1 GiB 落盘哈希、中途取消、独立启动退出、更新解析通过。
- [ ] P09 用户物理 Wi-Fi 反馈 5.1.2 约 11–14 MB/s，并确认发布。前 1–3 秒速度曲线与整批平均耗时尚未提供，严格对照仍未完成；不保证 TCP 首个数据包即可达到峰值。

首包探针模拟不返回 100 Continue 的接收端：5.1.1 信令请求含 Expect 头，正文等待 358.21 ms；5.1.2 不含该头，等待 0.01 ms。两版本文件流请求均没有 Expect 等待。该证据说明连接信令的一次等待被消除，不能解释全部无线爬升或推导持续吞吐提升。

本次本机回环随机样本：130 MiB 四文件用时 0.496 秒、约 275 MB/s；1 GiB 用时 2.144 秒、约 501 MB/s，含对方提交且 SHA-256 一致。仅用于排查明显回归，热缓存和机器负载影响很大。

代码与研究结论：

- [RFC 5681 §3.1](https://www.rfc-editor.org/rfc/rfc5681.html#section-3.1) 描述 TCP 用慢启动探测未知容量。新连接和部分空闲后传输仍需逐步探测；应用不能保证零时间达到无线极限。
- [微软 Expect100Continue 文档](https://learn.microsoft.com/en-us/dotnet/api/system.net.servicepoint.expect100continue?view=netframework-4.8.1) 解释 POST 的额外握手及连接创建前配置要求。本次通过探针确认 FindYou 信令受影响，文件流已关闭该行为。
- [LocalSend 发送实现](https://github.com/localsend/localsend/blob/main/app/lib/provider/network/send_provider.dart) 将文件列表交给后台上传任务并分别处理进度与完成事件；这支持继续保持后台传输与界面分离。没有从该源码得到“无限并发能立即跑满”的依据，本次未引入新的并发协议或依赖。

HTTP 近期速度来自应用读写已处理字节，受 socket/磁盘缓冲影响，不等于无线网卡的逐包测量。UI 仍沿用二进制 MB 单位；短于 250 ms 的数据使用最小观察区间以抑制瞬时虚高。诊断接口保持原字段，发送中的 rate 改为相同近期口径，完成后为 0。

## 证据与复测

2026-09-12，本机 Wi-Fi 协商发送 351 Mbps、接收 390 Mbps，TCP 自动调优 normal。无法读取另一台电脑的协商速率或磁盘性能。

两个隔离进程、本机回环、随机二进制样本（并非原 Word 文件），包含对方落盘确认：原版本四文件两轮 245–312 MB/s、1 GiB 两轮 210–603 MB/s；接收缓冲方案首轮分别约 276、309 MB/s。磁盘缓存、系统负载造成显著波动，不能据此声称提升百分比或 Wi-Fi 达标。这也不能排除远端设备、无线竞争、VPN 路由或文件安全扫描造成瓶颈。

双方运行 output/FindYou.Performance.exe（设置显示 5.1.1），复测同一个约 130 MB 文件夹。完成后记录“平均速度”，悬停完成状态查看总耗时、读取等待、网络写入等待、保存确认。平均速度包含扫描与对方提交时间；UI 沿用二进制 MB 单位，基准脚本 MB/s 为十进制。网络写入等待包含 TCP 背压，可能来自无线链路或接收侧磁盘，不能单独认定是哪一端。

最终构建复测四文件约 240 MB/s、1 GiB 约 321 MB/s，全部 SHA-256 一致；实际 HTTP 传输进行中取消与接收临时文件清理通过，独立程序启动退出通过。发送预读未保留，因此结果仅代表接收缓冲优化与诊断版。

传输统计日志仅添加协议、字节数和耗时，不包含文件名、路径或设备地址。P2P 的读写分项未记录，不应以零值解释为无耗时；界面对此仅显示总时间。

若仍约 6 MB/s，使用相同两台电脑和同一文件夹，将一台改为网线连接进行对照，同时记录另一台 Wi-Fi 协商速率。当前不能承诺原无线拓扑必达 20 MB/s，也不会将未确认的吞吐作为正式发布承诺。
