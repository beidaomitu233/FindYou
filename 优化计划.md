# FindYou 原生界面与传输优化

输入：现有文件互传协议、托盘服务、设备发现与用户确认的简洁雷达交互。
输出：不启动浏览器的 Windows 客户端、后台发送与接收、真实速度证据和跨网直传方案。

| 编号 | 状态 | 工作与验收 |
| --- | --- | --- |
| N01 | [x] | 读取传输调用链并测量旧版基线：16 MiB 回环 1.835 秒，SHA-256 一致。 |
| N02 | [x] | 默认界面已替换为原生 WPF，保留发现、互传、记录、设置和托盘入口；进程检查确认无浏览器子进程。 |
| N03 | [x] | 发送与接收已移入原生后台；文件、文件夹、空目录、中文路径与双向并发通过实际落盘校验。 |
| N04 | [x] | SIPSorcery 原生 WebRTC 已保留 DTLS 加密 P2P，发现服务器仍只交换信令；当前吞吐偏低，列为后续替换项。 |
| N05 | [ ] | 取消、断开、临时清理、同时连接和完整退出已通过；仍需两台物理电脑执行真实拖放、返回路径和视觉验收。 |
| N06 | [x] | 已完成协议原理、速度因素和服务器只会合的跨网高速方案，明确 NAT 与中继限制。 |

协议变更：增加经现有批次授权校验的单文件流式 HTTP 上传接口，既有分块接口保留；原生发送先探测对方能力。P2P 延续现有控制消息与二进制传输顺序。

基线仅为本机回环测试，不作为千兆网、Wi-Fi 或互联网吞吐承诺。真实互联网速度须受发送方上行、接收方下行、磁盘、丢包和路由共同约束。
