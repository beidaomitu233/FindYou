# FindYou 当前版本验证记录

## v5.1.2 传输起步与近期速度

2026-09-12，本机两个隔离进程验证：原生构建、双向 HTTP、中文文件夹/空文件/空目录、双方同时连接、P2P 16 MiB 窗口传输与双向完整性、取消清理通过。P2P 本次约 0.16 MiB/s，仍属于慢速回退，不纳入本次局域网提速声明。独立启动、令牌拒绝、退出和更新解析回归通过。

`output/http-startup-smoke.ps1` 使用本机 TCP 接收端，连续处理 GET 探测、信令 POST 和文件流 POST，不返回 100 Continue。5.1.1 的信令正文等待 358.21 ms；5.1.2 等待 0.01 ms，两个 POST 均无 Expect 头，三个请求共用同一 TCP 连接。旧版本文件流也已无 Expect 等待，不把信令收益扩大为整个传输提速。

`output/speed-smoke.ps1` 验证准备等待不稀释近期速度、恒速采样、停滞归零、恢复传输、并发更新和有界样本数量。HTTP 流式双方显示近期速度；P2P 与接收分块按原有确认粒度显示平均值。完成后平均速度保留落盘确认时间。页面改动通过构建与调用检查，未进行本次桌面视觉验收。

`output/throughput-smoke.cjs` 的 130 MiB 四文件用时 0.496 秒（约 275 MB/s），1 GiB 用时 2.144 秒（约 501 MB/s），含接收提交且 SHA-256 一致，进行中取消与临时目录清理通过。回环/缓存数据不能代表实际两机 Wi-Fi 或用来计算此次提升百分比。用户报告上一版约 11 MB/s，5.1.2 在物理 Wi-Fi 的起步表现待复测。

构建文件：`output/FindYou.Performance.exe`，程序集 5.1.2.0。该构建尚未发布到 GitHub Releases，公开版本仍为 5.1.0。研究来源与复测方法见 [传输性能计划.md](传输性能计划.md)。

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\build.ps1 -OutputPath .\output\FindYou.Performance.exe
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\output\speed-smoke.ps1
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\output\http-startup-smoke.ps1 -Exe .\output\FindYou.Performance.exe -RequireNoExpect
node .\output\throughput-smoke.cjs .\output\FindYou.Performance.exe startup-5.1.2
node .\output\native-smoke.cjs .\output\FindYou.Performance.exe
node .\output\package-smoke.cjs .\output\FindYou.Performance.exe
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\output\update-smoke.ps1 -Exe .\output\FindYou.Performance.exe
```

## v5.1.0 在线更新

2026-09-12：更新模块构建通过；`output/update-smoke.ps1` 验证当前版本、降级、正式新版本、预发布过滤、下载地址限制、不完整附件以及 SHA-256 一致/不一致分支通过。独立程序启动、管理接口令牌与退出回归通过。更新包下载后由用户退出并替换，不自动停止传输。`output/update-online-smoke.ps1` 已通过真实 GitHub 最新版本接口、EXE 下载以及与本地发布程序的 SHA-256 比较。设置页控件已接线并通过构建，尚未执行桌面点击和视觉验收。

验证日期：2026-09-10。环境：Windows，本机启动两个隔离的 FindYou 进程，分别使用独立数据目录、TCP 和 UDP 端口。文件结果使用 SHA-256 校验。

| 场景 | 实际结果 |
| --- | --- |
| 原生 WPF 构建和独立 EXE 启动 | 通过 |
| 根地址不再提供浏览器 UI | 通过，返回 403 文本 |
| 本机管理接口无令牌访问 | 拒绝 |
| HTTP 会话建立与双方自动进入连接状态 | 通过 |
| 双方同时点击连接时会话编号收敛 | 通过 |
| HTTP 双向同时发送 | 通过 |
| 文件夹、空文件、空文件夹和中文路径 | 通过 |
| 原生 P2P 建立与 16 MiB 文件传输 | 通过 |
| P2P 双向同时发送及 SHA-256 完整性 | 通过 |
| 取消传输和临时文件清理 | 通过 |
| 自定义托盘图标、窗口复用和退出链路 | 通过构建与进程检查 |
| 启动新增浏览器或 WebView2 进程 | 0 个 |
| 退出后 FindYou 进程残留 | 0 个，退出码 0 |
| 旧配置携带 `fw=1` 时的独立 EXE 启动 | 通过；程序不再以该字段代替当前路径规则核对 |
| EXE 路径变化后的单实例识别 | 通过构建与代码路径检查；不同路径使用不同唤醒事件并提示退出旧实例 |

性能样本：旧页面分块路径传输 16 MiB 用时 1.835 秒，约 8.72 MiB/s；最终程序的原生 HTTP 流式路径最近一次用时 0.131 秒，约 122.22 MiB/s。原生 P2P 同机 16 MiB 多次样本约 0.17–0.25 MiB/s，属于仍需替换的慢速回退路径。样本均为本机回环和热缓存，不代表真实网络吞吐。

内存样本：最终程序的原生窗口稳定后为 123.3–135.3 MiB 工作集、134.6–138.5 MiB 私有内存；无界面托盘服务约 34.6 MiB 工作集、23.8 MiB 私有内存。测量脚本同时确认未创建 `msedge` 或 `msedgewebview2` 进程。

验证命令：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\build.ps1 -OutputPath .\output\FindYou.Native.exe
node .\output\native-smoke.cjs .\output\FindYou.Native.exe
node .\output\package-smoke.cjs .\output\FindYou.Native.exe
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\output\memory-smoke.ps1 -Exe .\output\FindYou.Native.exe
```

本次根据两台电脑出现“单向发现、无法连接，旧路径版本可用”的现场结果，修正了防火墙规则随 EXE 路径迁移及新旧实例冲突。修复后的版本仍需在这两台物理电脑上复测自动 UDP 发现和 TCP 连接。真实拖放、窗口视觉、千兆网/Wi-Fi 吞吐，以及不同运营商、IPv6、对称 NAT 和企业防火墙下的跨网成功率也未完成现场验证，因此不标记为通过。
