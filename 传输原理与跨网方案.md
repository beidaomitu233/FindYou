# FindYou 传输原理与跨网直连方案

## 当前实现

FindYou 由一个 Windows 托盘进程完成设备发现、连接、文件授权、发送与接收。界面已经改为原生 WPF，同一进程内直接调用业务层；启动和退出都不再依赖 WebView2 或系统浏览器。

连接时先探测局域网地址。能直连时使用 HTTP/1.1 over TCP，一份文件对应一次流式 POST，发送端以 128 KiB 缓冲区顺序读取，接收端同步写入临时批次目录。完整批次提交后才移动到接收目录；中断、取消或长度不符会中止批次并清理临时内容。双方拥有独立发送队列，所以可以同时发送文件、文件夹、空文件夹和中文路径。

局域网 HTTP 探测失败后，应用通过发现通道交换 SDP 和 ICE 候选，使用 SIPSorcery 建立 WebRTC DataChannel。协议栈为 SCTP → DTLS → ICE/UDP，文件数据经 DTLS 加密并直接到达对方。控制消息负责开始批次、创建条目、确认窗口、提交和取消；二进制消息承载文件内容。[RFC 8831](https://www.rfc-editor.org/rfc/rfc8831.html) 描述了该协议栈和消息边界要求。

设备发现有三种来源：局域网 UDP 广播、可选的 MQTT over WebSocket 公共发现通道，以及自托管 `findyou-relay.js`。后两者只交换设备和连接信令，不接收文件字节。

## 旧路径为什么慢

旧版局域网发送链路是：磁盘读取 → 本机 HTTP 接口 → JavaScript 缓冲 → 本机 HTTP 代理 → 对方 HTTP 接口 → 每块等待确认。它会重复跨越进程内 Web API 边界，产生数组复制、请求解析、文件重复打开和按块往返等待。旧版本每 256 KiB 等一次确认，延迟为 50 ms 时，仅窗口等待的理论上限约为 5 MiB/s。

新路径从后台服务直接读取磁盘并持续写入同一个远端请求，去掉页面和逐块请求。兼容旧端时仍会退回 1 MiB 分块接口。`Expect: 100-continue` 已禁用，TCP Nagle 已关闭；它们可能影响小块延迟，但现有证据不足以把旧版慢速归因于其中任何单项。

本机回环、热缓存、16 MiB 随机文件的实测结果：旧页面路径 1.835 秒，约 8.72 MiB/s；最终程序的原生 HTTP 流式路径最近一次 0.131 秒，约 122.22 MiB/s，SHA-256 一致。这个结果说明应用层额外往返已经消除，不能代表真实网线、Wi-Fi 或公网速度。

原生 WebRTC 回退路径也用 16 MiB 随机文件完成了双向并发与哈希校验，但同机多次样本仅约 0.17–0.25 MiB/s。当前纯托管 SCTP 实现存在明显吞吐限制，适合作为连通性回退，尚不能承担高速跨网传输。发送窗口从 256 KiB 提升到 4 MiB 后，瓶颈仍在底层 DataChannel 实现。

实际速度取以下项目中的最低值：发送方磁盘读取、发送方上行、接收方下行、接收方磁盘写入、网卡或 Wi-Fi、CPU 加密能力，以及丢包与往返延迟。运营商标注的 Mbps 除以 8，才约等于理想状态下的 MB/s；协议开销和丢包会继续降低结果。

## 跨网络且不消耗服务器文件带宽

可行架构是“服务器只会合，终端直传”：

1. 两台设备以配对码进入同一信令房间，服务器只保存短时在线信息并转发 offer、answer 和候选地址。
2. 终端收集本机 IPv4、IPv6 和 STUN 映射地址，通过 ICE 逐对进行连通性检查。[RFC 8445](https://www.rfc-editor.org/rfc/rfc8445.html) 规定了这一候选交换和检查过程。
3. 选中直达路径后，文件字节直接在双方公网链路上传输。此时服务器带宽只承担很小的信令流量，文件速度由两端网络和设备决定。
4. 会话令牌绑定双方设备身份；文件批次继续使用现有路径授权、临时落盘、提交和取消语义。

该方案无法保证所有网络都直连。双方处于严格对称 NAT、运营商级 NAT、企业防火墙或 UDP 被封锁时，只有公网 IPv6、路由器端口映射，或 TURN/文件中继能提高成功率。TURN 的定义就是让服务器转发数据，[RFC 8656](https://www.rfc-editor.org/rfc/rfc8656.html) 所以一旦走 TURN，文件速度必然受服务器带宽影响。若产品坚持服务器永不接触文件，应在直连失败时明确提示，并让用户选择开启 IPv6、端口映射或换网络。

## 高速跨网传输的实施建议

短期应保留现在的 HTTP 流作为局域网主路径，并把纯托管 P2P 标为兼容回退。下一项实现建议是将 P2P 数据层替换为原生 `libdatachannel` 的无媒体构建：它支持 Windows、ICE/STUN/TURN、DTLS 和 SCTP，同时避免浏览器运行时；项目官方说明见 [libdatachannel](https://github.com/paullouisageneau/libdatachannel)。替换后必须以两台物理电脑、不同运营商网络和至少 1 GiB 文件重新压测，再决定窗口、并发数和默认策略。

对拥有公网 IPv6 或端口映射的设备，可进一步增加 MsQuic 数据通道。MsQuic 提供 TLS 1.3、多流、拥塞控制和丢包恢复，并以高吞吐为目标，见 [Microsoft MsQuic](https://github.com/microsoft/msquic)。QUIC 本身不解决 NAT 穿透，仍需先得到可达地址；因此它应作为已建立直达路径后的高速数据层，不能取代 ICE/STUN。

建议验收指标为：千兆局域网大文件达到链路有效吞吐的 80% 以上；跨网直连速度达到两端可用上下行较小值的 70% 以上；1 GiB 文件哈希一致；双向并发、取消、断线清理、同名另存和内存上限同时通过。未完成物理网络测试前，不写固定速度承诺。

## 原生客户端内存与退出

当前界面由 WinForms 窗口承载 WPF，不创建浏览器渲染、GPU 或 WebView2 子进程。发送端复用 128 KiB HTTP 缓冲区，P2P 的应用窗口上限为 4 MiB，已完成任务释放会话引用，隐藏窗口后停止界面刷新和雷达动画。

2026-09-09 的隔离进程测量中，最终程序的原生窗口稳定后工作集为 123.3–135.3 MiB、私有内存为 134.6–138.5 MiB；`/noui` 托盘服务工作集约 34.6 MiB、私有内存约 23.8 MiB。测量均未新增 `msedge` 或 `msedgewebview2` 进程，调用退出后进程在 10 秒内以退出码 0 结束。数值会随系统字体缓存、网络组件和历史记录变化。
