# FindYou 发布签名

当前 FindYou.exe 经检查为 NotSigned。未签名会导致发布者身份无法验证，但仅凭这一项不能确定浏览器所有拦截的原因。

## 正式发布流程

1. 使用真实发布者身份申请公开信任的代码签名证书，或使用支持该身份地区的托管签名服务。自签证书不能解决公众下载的信任问题。
2. 完成构建和传输验收后签名。证书由 Windows 证书存储或硬件设备管理，私钥和密码不放进项目。
3. 对已签名文件验证签名和时间戳，再通过稳定的 HTTPS 下载地址分发。同一发布版本复用同一个文件，签名后不能重新编译。

对于当前用户证书存储中已有的可信代码签名证书，安装 Windows SDK 的 SignTool 后执行：

```powershell
.\sign-release.ps1 -CertificateThumbprint '<证书指纹>' -SignToolPath '<signtool.exe 的完整路径>'
```

脚本校验证书用途、私钥可用性和信任链，执行 SHA-256 签名及 RFC 3161 时间戳，随后验证签名。托管云签名服务需要按其认证方式单独接入，此脚本不代替该服务的身份验证。

## 拦截类型

- “未知发布者”：核对 Authenticode 数字签名。
- “不常下载”或“无法识别的应用”：属于应用信誉问题。可信签名有助于确认身份，但新文件仍可能有信誉提示，不保证签名后立即消失。
- “病毒”“恶意软件”：核对具体检测名和文件哈希，确认误报后向对应安全厂商提交复核。
- “不安全下载”：核对实际下载 URL、HTTPS 和重定向链。

不关闭用户安全防护，不通过删除下载来源标记或改扩展名来规避检查。若选择 Microsoft Store 的 MSIX 分发，需要另外完成打包适配和商店认证；当前项目尚未完成该流程。

官方依据：[SmartScreen 与应用信誉](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation)、[开发者误报复核说明](https://learn.microsoft.com/en-us/defender-xdr/developer-faq)。
