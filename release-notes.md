# FindYou v5.1.2

Windows x64 双向文件与文件夹互传工具，使用 .NET Framework 4.8 原生 WPF 界面。

- 合并接收端的小块磁盘写入，改善局域网文件夹与大文件传输，保留完整落盘确认。
- 消除首次连接信令的额外 HTTP 等待；本机探针从约 358 毫秒降至不足 1 毫秒。
- 局域网传输显示最近约 1 秒的速度，完成后显示包含保存确认的平均速度，并可查看发送阶段耗时。
- 修正接收速度变化时的界面刷新；保留双向传输、取消清理和 P2P 回退。

现场用户反馈局域网 Wi-Fi 约 11–14 MB/s；速度取决于网络与磁盘，不承诺所有环境达到固定吞吐。130 MiB 文件夹、1 GiB 文件的落盘哈希、双向传输、中文与空目录、取消清理及 P2P 回归通过。

升级：设置 → 软件更新 → 检查更新。下载完成后等待传输结束，从托盘退出，再替换原程序；建议双方升级。更新不会自动安装或中断传输。

下载 FindYou.exe。FindYou.exe.sha256 用于完整性校验。依赖许可见随版本附带的 THIRD-PARTY-NOTICES.txt 和 SIPSorcery-LICENSE.md。

本版本尚未使用可信代码签名证书，浏览器或 Windows 仍可能显示未知发布者或下载信誉提示。更新校验不等同于发布者数字签名。原生 P2P 吞吐仍有优化空间，优先使用局域网直传。
